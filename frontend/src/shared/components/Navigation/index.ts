/**
 * Navigation Family - Barrel Export
 *
 * Exporte tous les composants et types de la famille Navigation
 */

export { TabGroup } from './TabGroup';
export type { Tab, TabGroupProps } from './TabGroup';

export { PaginationBar } from './PaginationBar';
// Note: PaginationBarProps n'est pas exporté dans le fichier source
