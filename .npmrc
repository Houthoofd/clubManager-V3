# pnpm configuration

# ============================================================
# HOISTING - Single root node_modules
# ============================================================
# Hoist all dependencies to root node_modules
shamefully-hoist=true

# Use hoisted node linker (single node_modules at root)
node-linker=hoisted

# ============================================================
# WORKSPACE SETTINGS
# ============================================================
# Use workspace protocol in package.json
save-workspace-protocol=true

# Auto-install peer dependencies
auto-install-peers=true

# Strict peer dependencies (false pour éviter erreurs avec certains packages)
strict-peer-dependencies=false

# ============================================================
# LOCKFILE
# ============================================================
lockfile=true

# ============================================================
# STORE
# ============================================================
# Store location (optionnel - décommenter si besoin)
# store-dir=~/.pnpm-store

# ============================================================
# SCRIPTS
# ============================================================
# Enable pnpm to use git-hosted packages
enable-pre-post-scripts=true
