feat(messaging): implemente le module messagerie interne et email

DB - Migration 005
- Nouvelle table broadcasts : trace les envois groupes
- Colonne broadcast_id sur messages : lien vers le broadcast
- Colonne envoye_par_email sur messages

Backend - Clean Architecture
- IMessagingRepository: 9 methodes (send, inbox, sent, read, delete)
- MessagingEmailService: wrapper Resend pour notifications de message
- SendMessageUseCase: individuel (1:1) + broadcast (par role/tous)
- GetInboxUseCase, GetSentUseCase: liste paginee
- GetMessageUseCase: detail + auto-mark as read
- GetUnreadCountUseCase: compteur non-lus
- DeleteMessageUseCase: supprime pour le destinataire uniquement
- MySQLMessagingRepository: 9 methodes SQL avec JOIN utilisateurs
- MessagingController + messagingRoutes (6 endpoints)
- app.ts: montage sur /api/messages

Frontend
- messagingApi.ts: getInbox, getSent, getMessage, getUnreadCount, sendMessage, deleteMessage
- messagingStore.ts: Zustand avec inbox/sent/selected/unreadCount/tabs
- useMessaging hook: charge inbox + unread au montage
- useUnreadCount hook: polling toutes les 30 secondes
- ComposeModal: 3 types destinataires (individuel/tous/role)
- MessageListItem: point bleu non-lu, badge Groupe, icone email, date relative
- MessageDetail: metadonnees, contenu, suppression avec confirmation
- MessagesPage: layout 3 colonnes (sidebar/liste/detail), skeleton loader, pagination
- App.tsx: import reel MessagesPage remplace placeholder
- PrivateLayout.tsx: badge rouge non-lus sur item Messages
