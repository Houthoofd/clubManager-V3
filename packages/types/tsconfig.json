{
  "compilerOptions": {
    "target": "ES2020",  // Version de JavaScript vers laquelle le code sera compilé
    "module": "NodeNext", // Passez en ESM pour des exports compatibles import/export
    "moduleResolution": "NodeNext",  // Résolution des modules de manière compatible avec Node.js
    "esModuleInterop": true,  // Permet l'importation de modules CommonJS via des importations ES6
    "skipLibCheck": true,  // Ignore la vérification des fichiers de type dans les bibliothèques (améliore la vitesse de compilation)
    "forceConsistentCasingInFileNames": true,  // Force une casse cohérente pour les noms de fichiers
    "strict": true,  // Active toutes les options strictes de TypeScript (recommandé pour la sécurité des types)
    "outDir": "./dist",  // Dossier de sortie de la compilation
    "rootDir": "./src",  // Dossier racine des sources TypeScript
    "baseUrl": ".",  // Base URL pour les imports relatifs
    "paths": {
      "types/*": ["./types/*"]  // Si vous avez un dossier "types" séparé, configurez ici les alias pour le dossier types
    },
    "resolveJsonModule": true,  // Permet d'importer des fichiers JSON en tant que modules
    "lib": ["es2020", "dom"],  // Spécifie les bibliothèques à inclure lors de la compilation (ajoutez "dom" si vous interagissez avec des API Web dans Node.js, sinon omettez-le)
    "declaration": true,  // Crée les fichiers de déclaration `.d.ts` lors de la compilation
    "declarationMap": true,  // Crée les cartes de déclaration pour le débogage des fichiers `.d.ts`
    "verbatimModuleSyntax": true // (TS 4.7+) conserve l'extension .js dans les imports/exports ESM générés
  },
  "include": [
    "src/**/*",  // Inclut tous les fichiers dans le dossier src
    "types/**/*"  // Inclut tous les fichiers de types externes si nécessaire
  ],
  "exclude": [
    "node_modules",  // Exclut le dossier node_modules pour éviter de compiler des fichiers tiers
    "dist"  // Exclut le dossier dist où la compilation se fait
  ]
}
// La configuration est correcte pour générer des imports/exports ESM avec .js
// Rien à modifier ici.