# E2E package — public npm registry only
# Aucune dépendance GitHub Packages ici.
# Ce fichier override le .npmrc racine pour supprimer le warning
# "Failed to replace env in config: ${GITHUB_PACKAGES_TOKEN}".
//npm.pkg.github.com/:_authToken=
